import { METRICS } from './art';

/**
 * Два варианта одного устройства. Отличаются заметно, а не на волос: поле
 * плашки вдвое, просвет скобы вдвое, и скоба накрывает четыре буквы из семи
 * против двух.
 */
export type Variant = {
  id: string;
  no: number;
  name: string;
  /** Числа сетки, которыми она отличается. */
  spec: string;
  diff: string;
  weak: string;
};

export const VARIANTS: Variant[] = [
  {
    id: 'v1', no: 1, name: 'Просторный',
    spec: 'поле плашки 8 модулей · просвет скобы 4 · скоба на четыре буквы',
    diff: 'Скоба держит «ЭЛЕМ» и отпускает «ЕНТ» на открытое поле; вокруг всего — поле в восемь модулей, вдвое шире тесного. Отношение сторон 2,7 : 1, почти вдвое ближе к квадрату.',
    weak: 'Плашка забирает много места вокруг слова, и на мелком логотип читается прежде всего тёмным прямоугольником: буквы приходят вторым планом.',
  },
  {
    id: 'v2', no: 2, name: 'Тесный',
    spec: 'поле плашки 4 модуля · просвет скобы 2 · скоба на две буквы',
    diff: 'Поле и просвет вдвое у́же, скоба накрывает только «ЭЛ». Слово занимает плашку почти целиком, отношение сторон 3,3 : 1 — вытянутый брусок, который ложится в строку.',
    weak: 'При просвете в два модуля верхнее плечо скобы идёт вплотную над буквами, и на высоте шапки сайта просвет — меньше полутора пикселей: скоба почти касается слова.',
  },
];

/** Кегль шапки сайта: t3 = 21 px в CoFo Sans, высота прописных в нём 14,28 px.
 *  Логотип ставится в тот же рост прописных — это число про шапку, а не про
 *  гарнитуру логотипа: у неё своя высота прописных, METRICS.cap. */
export const HEADER_CAP = 14.28;

/** Числа модульной сетки для показа на странице. */
export const GRID: [string, string][] = [
  ['Модуль', `${METRICS.module} единиц — от него взяты все остальные`],
  ['Высота прописных', `${METRICS.cap} = 18 модулей`],
  ['Толщина штриха', `${METRICS.stem} = 4 модуля, она же у скобы`],
  ['Просветы внутри букв', `${METRICS.gap} = 3 модуля и ${METRICS.gap + METRICS.module} = 4`],
  ['Наружная фаска', `${METRICS.chamfer} = 3 модуля, угол 45°`],
  ['Внутренняя фаска', `${METRICS.chamferInner} = 0,657 модуля — параллельный контур, c − (2 − √2)·s`],
];
