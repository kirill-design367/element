import { METRICS } from './art';

/**
 * Одно устройство, два варианта. Отличаются пропорциями, не идеей:
 * глубина фаски, длина плеч (она же — сколько букв накрывает скоба), вес
 * скобы к штриху буквы, поля плашки и просвет скобы над буквами.
 *
 * Доли, по которым всё построено, заданы в scripts/build-marks.py; здесь
 * они повторены словами, чтобы разбор читался рядом с картинкой.
 */

export type WordSet = 'caps' | 'mixed';

export type Variant = {
  id: string;
  no: number;
  name: string;
  /** Доли, которыми она отличается от опорной. */
  spec: string;
  /** Чем отличается от соседних. */
  diff: string;
  /** Где слабое место. */
  weak: string;
};

export const VARIANTS: Variant[] = [
  {
    id: 'l1', no: 1, name: 'Просторный',
    spec: 'фаска 0,14 · плечи 0,62 и 0,30 · вес скобы 1,00 · поле 0,42 · просвет 0,22',
    diff: 'Скоба накрывает четыре буквы из семи, верхнее плечо вдвое длиннее нижнего, вес скобы равен штриху буквы, поле плашки вдвое шире, чем у тесного.',
    weak: 'Скоба обрывается на середине слова, и «ЭЛЕМ» внутри читается отдельно от «ЕНТ» снаружи. Это и есть замысел, но глаз каждый раз проверяет, не разорвано ли слово.',
  },
  {
    id: 'l8', no: 2, name: 'Тесный',
    spec: 'фаска 0,14 · плечи 0,62 и 0,30 · вес скобы 1,14 · поле 0,24 · просвет 0,14',
    diff: 'Поле плашки урезано почти вдвое, просвет скобы над буквами тоже, штрих скобы тяжелее буквенного. Самая плотная из восьми.',
    weak: 'Скоба тяжелее букв и перетягивает внимание на себя; при поле 0,24 её плечи почти касаются края плашки.',
  },
];


/** Кегль шапки сайта: t3 = 21 px, Black. Высота прописных в нём — 14,28 px. */
export const HEADER_CAP = (21 * METRICS.cap) / METRICS.upem;

/** Доли, общие для всех отрисовок. Повторены в отчёте. */
export const COMMON = [
  ['Угол фаски', '45°, одна и та же линия у скобы и у буквы Э'],
  ['Ширина штриха буквы', '176 единиц, 0,259 высоты прописных — измерена по Н'],
  ['Отступ вертикали скобы от слова', '0,30 высоты прописных'],
  ['Высота скобы изнутри', 'высота прописных плюс два просвета'],
] as const;
