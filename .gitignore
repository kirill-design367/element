node_modules/
.next/
out/
.env*
!.env.example
*.tsbuildinfo
next-env.d.ts
.DS_Store
lighthouse/
screenshots/
__pycache__/

# Настройки приёмника заявки: собираются при сборке из lib/company.ts
# скриптом scripts/build-mail-config.mjs. В историю не идут — адрес
# получателя меняется в одном месте, в lib/company.ts.
public/api/config.php

# Снимок данных из админки: скачивается при сборке, в историю не идёт.
cms-data.json

# Метка версии данных: пишется при сборке, сверяется на боевом адресе.
public/cms-version.txt
