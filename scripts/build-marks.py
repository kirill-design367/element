#!/usr/bin/env python3
"""Геометрия логотипа для служебной страницы /logo.

Самодельная гарнитура убрана целиком: нарисованные буквы оказались
нечитаемыми — Э превращалась в цифру три. Правило «ни одной кривой»,
применённое к самим буквам, и было причиной.

Слово будет набираться готовым шрифтом; подключается он следующим коммитом.
Здесь не остаётся ничего: ни букв, ни скобы, ни плашки — скоба и плашка
строились от модуля самодельной гарнитуры и без неё чисел не имеют.

Пересобрать:  python3 scripts/build-marks.py
"""


def main():
    print('геометрия ещё не собрана: шрифт подключается следующим коммитом')


if __name__ == '__main__':
    main()
