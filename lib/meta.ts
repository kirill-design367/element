/**
 * ЗАГОЛОВКИ И ОПИСАНИЯ СТРАНИЦ.
 *
 * Раньше лежали двумя литералами внутри app/layout.tsx и
 * app/catalog/page.tsx. Это тот же редактируемый текст, что и всё
 * остальное на сайте, и место ему рядом с данными, а не в разметке.
 *
 * Числа сюда не вписываются: сколько позиций и в скольких группах —
 * считается из catalog.ts, срок отсрочки — из pricing.ts. Здесь только то,
 * что пишет человек.
 */

import { DEFERRAL_DAYS } from './pricing';

export interface PageMeta {
  title: string;
  description: string;
}

export const META: {
  /** Заголовок вкладки на лендинге. */
  home: PageMeta;
  /** Шаблон для внутренних страниц: %s подставляет их собственный title. */
  titleTemplate: string;
  catalog: PageMeta;
} = {
  home: {
    title: 'Строительный Дом Элемент — щебень, песок, ПГС, грунт и металлопрокат, самовывоз из Люберец',
    /* Срок отсрочки не набирается второй раз: он стоит в условии для юрлиц и
       здесь, и раньше это были два независимых числа. */
    description:
      `Отгружаем с площадки в Люберцах: щебень, песок, ПГС, отсев, чернозём, металлопрокат. Отсрочка до ${DEFERRAL_DAYS} дней, договор поставки, закрывающие документы. Цену, рейсы и время считаете на сайте.`,
  },
  titleTemplate: '%s — Строительный Дом Элемент',
  catalog: {
    title: 'Каталог инертных материалов',
    description:
      'Щебень гранитный, известняковый и гравийный, песок карьерный и мытый, ПГС, отсев, чернозём. Цены самовывозом за м³ и за тонну, фракции, ГОСТ, наличие.',
  },
};

/**
 * СЛУЖЕБНЫХ СТРАНИЦ У САЙТА БОЛЬШЕ НЕТ.
 *
 * Их было две — сравнение шрифтовых пар и витрина логотипа, — обе
 * открывались прямым адресом и покупателю не предназначались. Обе удалены
 * 30.08: выбор сделан, витрины отработали.
 *
 * Список путей жил здесь, потому что был один на два механизма: мета-тег
 * `noindex` на самих страницах и запрет в robots.txt. Держать пустой массив
 * незачем — `Disallow:` с пустым значением означает «можно всё», то есть
 * ровно то же, что отсутствие строки, только с видом работающего правила.
 * Появится новая служебная страница — список заводится здесь заново, и
 * robots.ts берёт его отсюда.
 */
