/** Один источник правды по анимациям: длительности, easing и режим покоя. */

export const DUR = {
  /** Микровзаимодействия: наведение, фокус, смена плотности фона. */
  hover: 0.3,
  /** Служебные переходы интерфейса. */
  base: 0.4,
  /** Появление блока при входе в кадр. Короче 0,4 с не бывает ничего. */
  reveal: 0.9,
  /** Набор цифры в счётчике при появлении в кадре. */
  count: 1.4,
  /** Добегание итога расчёта до нового значения при правке поля. */
  recount: 0.6,
  /** Перелёт карточки категории в каталог. */
  flip: 0.62,
} as const;

/** Единственное появление на сайте: прозрачность и масштаб, без сдвига. */
export const REVEAL = {
  /** Начальный масштаб. Ближе к единице — заметно, но не «выпрыгивает». */
  scale: 0.98,
  /** Шаг между соседними элементами одного блока. */
  stagger: 0.07,
  /** Порог входа в кадр. */
  start: 'top 85%',
} as const;

/**
 * Начало отрезка параллакса, % от высоты кадра.
 *
 * Число живёт здесь, а не в двух местах, потому что его знают двое: разметка
 * кадра (components/ui/Photo.tsx ставит его инлайном, чтобы кадр рождался
 * уже в этом положении) и Motion.tsx (строит от него отрезок до +2%).
 * Разойдясь на десятую, они дают скачок кадра при подъёме движения — ровно
 * тот, ради которого сдвиг и заведён.
 */
export const PARALLAX_FROM_PERCENT = -2;

export const EASE = 'power3.out';
/** Easing появления. Мягче power3: у масштаба резкий выход читается рывком. */
export const EASE_REVEAL = 'power2.out';
export const EASE_INOUT = 'power3.inOut';

/** Пользователь просил не двигать интерфейс — уважаем на каждом входе. */
export function prefersReducedMotion(): boolean {
  if (typeof window === 'undefined') return false;
  return window.matchMedia('(prefers-reduced-motion: reduce)').matches;
}
