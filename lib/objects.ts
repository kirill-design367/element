/**
 * ОБЪЕКТЫ, НА КОТОРЫЕ ПОСТАВЛЯЛИ.
 *
 * Сами записи лежат в `lib/data/objects.data.ts` — их правит заказчик в
 * админке, и перед сборкой файл пересобирается из базы. Здесь остаётся
 * только переэкспорт: компонент Objects.tsx как читал отсюда, так и читает,
 * и про расшивку не знает ничего.
 */

export type { Project } from './data/types';
export { OBJECTS } from './data/objects.data';
