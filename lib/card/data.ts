/**
 * СОДЕРЖИМОЕ ДОКУМЕНТА: ВИЗИТКА И КАРТОЧКА ОРГАНИЗАЦИИ.
 *
 * Собирается из тех же файлов данных, что и сайт: ни одного числа и ни одной
 * характеристики здесь не набрано руками. Пороги и сроки — `lib/pricing.ts`,
 * контакты и реквизиты — `lib/company.ts`.
 *
 * Файл лежит в `lib/`, а не в `app/`: папка с именем маршрута, за которой нет
 * страницы, читается как забытый маршрут. Прежняя визитка жила в
 * `app/logo/card/` и уехала вместе с витриной знака 30.08.
 */

import { COMPANY, REQUISITES, SHIPPING_HOURS_SHORT } from '@/lib/company';
import { num, typo } from '@/lib/format';
import { DEFERRAL_DAYS, MIN_ORDER_M3, PRICE_HOLD_DAYS } from '@/lib/pricing';
import { qrMatrix, qrPath } from '@/lib/qr';

/** Три ключевых факта: число, единица и подпись. */
export type Fact = { value: string; unit: string; label: string };

/**
 * Три показателя — главный блок визитки. Подписи звучат условиями работы, а
 * не справкой: «минимальная партия отгрузки», а не «минимальная отгрузка».
 */
export const FACTS: Fact[] = [
  { value: num(MIN_ORDER_M3), unit: 'м³', label: 'минимальная партия отгрузки' },
  { value: num(DEFERRAL_DAYS), unit: 'дней', label: 'отсрочка платежа для юрлиц' },
  { value: num(PRICE_HOLD_DAYS), unit: 'дней', label: 'держим названную цену' },
];

/** Условия отгрузки одной строкой — то же, что в контактах сайта. */
export const SHIPPING = COMPANY.shipping;

/**
 * Призыв к действию. Обещает ровно то, что компания и делает по шагам
 * работы: цену обсуждают по телефону, время погрузки согласуют.
 */
export const CALL = typo('Звоните с объёмом и сроками — обсудим цену и согласуем время погрузки');

/**
 * Заголовок-предложение. Тот же текст, что в заголовке первого экрана:
 * держать его в двух местах нельзя, поэтому он объявлен здесь один раз.
 */
export const OFFER = typo('Щебень, песок, грунт и металлопрокат с отгрузкой без очереди');

export const CONTACTS = {
  phone: COMPANY.phoneLabel,
  phoneHref: `tel:${COMPANY.phone}`,
  address: COMPANY.address,
  /** Короткая форма для футера: «Отгрузка круглосуточно». */
  hours: `Отгрузка ${SHIPPING_HOURS_SHORT}`,
  /** Адрес сайта. Пусто — не рисуется ни строка, ни QR-код. */
  site: COMPANY.site,
  email: COMPANY.email,
};

/**
 * QR-код на сайт. Считается ПРИ СБОРКЕ: сайт статический, адрес известен
 * заранее, и клиентского кода для этого не нужно вовсе. Пустой адрес даёт
 * пустую матрицу, и код не рисуется.
 */
const QR_MATRIX = qrMatrix(COMPANY.site);
export const QR = QR_MATRIX.length
  ? { size: QR_MATRIX.length, path: qrPath(QR_MATRIX) }
  : null;

/** Строка реквизита: подпись, значение и признак «это число». */
export type Row = { term: string; value: string; digits?: boolean };

/**
 * РЕКВИЗИТЫ ОРГАНИЗАЦИИ.
 *
 * НАИМЕНОВАНИЯ ЗДЕСЬ НЕТ, И ЭТО НЕ ПРОПУСК: оно стоит заголовком листа, а
 * второй раз появляется только полем «Получатель» платёжного блока, где оно
 * реквизит платежа, а не имя документа. Повторять его третий раз строкой
 * реестра значило бы то же самое дублирование, из-за которого на официальной
 * бумаге однажды получают неправильный ответ на вопрос «а какое настоящее».
 *
 * Почты тут тоже нет: она контакт, а не реквизит, и стоит в подвале листа.
 */
export const ORG: Row[] = [
  { term: 'ИНН', value: COMPANY.inn, digits: true },
  { term: 'КПП', value: COMPANY.kpp, digits: true },
  { term: 'ОГРН', value: COMPANY.ogrn, digits: true },
  { term: REQUISITES.directorTitle, value: REQUISITES.director },
];

/**
 * БАНКОВСКИЕ РЕКВИЗИТЫ. ПОРЯДОК СТРОК НЕ МЕНЯТЬ: он повторяет порядок полей
 * платёжного поручения, чтобы бухгалтер переписывал сверху вниз, не
 * перескакивая глазами.
 *
 * ИНН и КПП в конце — БАНКА, а не наши. Рядом с нашими их перепутать в
 * платёжке проще простого, поэтому они стоят в этом блоке, а под блоком —
 * строка, которая говорит об этом словами.
 */
export const BANK: Row[] = [
  { term: 'Получатель', value: COMPANY.legalName },
  { term: 'Расчётный счёт', value: COMPANY.account, digits: true },
  { term: 'Банк получателя', value: COMPANY.bank },
  { term: 'БИК', value: COMPANY.bik, digits: true },
  { term: 'Корреспондентский счёт', value: COMPANY.corr, digits: true },
  { term: 'ИНН банка', value: REQUISITES.bankInn, digits: true },
  { term: 'КПП банка', value: REQUISITES.bankKpp, digits: true },
];

/**
 * ДВА АДРЕСА — РЯДОМ И ПОД ПОЛНЫМИ ПОДПИСЯМИ.
 *
 * Это главное требование заказчика к листу: юридический адрес и адрес
 * отгрузки не должны путать. Разводит их не цвет — в чёрно-белой печати
 * цвета нет вовсе, а лист уходит почтой, — а две вещи: подписи полными
 * словами и соседство, при котором различие видно раньше, чем прочитан
 * текст.
 *
 * СТРОКИ НАЗНАЧЕНИЯ ПОД АДРЕСАМИ УБРАНЫ 14.09 решением заказчика: подписи
 * «Юридический адрес» и «Адрес площадки» объясняют то же самое и короче.
 * Возвращать их не надо — место ушло кадру и кеглям.
 *
 * Сокращения «юр./факт.» не годятся: в сером у них нет второго признака,
 * который бы их подстраховал.
 */
export type AddressRow = { term: string; value: string };

export const ADDRESSES: AddressRow[] = [
  { term: 'Юридический адрес', value: REQUISITES.legalAddress },
  { term: 'Адрес площадки', value: COMPANY.address },
];

export const LEGAL_NAME = COMPANY.legalName;
export const BRAND_NAME = COMPANY.shortName;
