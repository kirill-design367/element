/**
 * БЕГУЩАЯ СТРОКА ПОД ПЕРВЫМ ЭКРАНОМ.
 *
 * Три факта, которые снабженец проверяет первым делом: куда возим, сколько
 * минимум и какие документы. Числа не вписаны в текст — они приходят из
 * pricing.ts, где живут все условия поставки: радиус и минимальная отгрузка
 * не должны разойтись с калькулятором.
 */

import { MAX_KM, MIN_ORDER_M3 } from './pricing';

export interface Fact {
  /** Подпись факта. */
  label: string;
  /** Сам факт. */
  value: string;
}

export const FACTS: Fact[] = [
  { label: 'География', value: `Москва и область, до ${MAX_KM} км от МКАД` },
  { label: 'Минимальная отгрузка', value: `${MIN_ORDER_M3} м³ — одна машина` },
  { label: 'Документы', value: 'Паспорт качества на каждую партию' },
];
