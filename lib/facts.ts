/**
 * БЕГУЩАЯ СТРОКА ПОД ПЕРВЫМ ЭКРАНОМ.
 *
 * Три факта, которые снабженец проверяет первым делом: куда ехать, сколько
 * минимум и какие документы.
 *
 * Первый факт был про доставку — «Москва и область, до 150 км от МКАД». Возим
 * мы теперь только на самовывоз, и радиус не значит ничего: значение имеет
 * то, где стоит площадка и когда она грузит. Числа и часы не вписаны в текст:
 * минимальная отгрузка приходит из pricing.ts, часы — из company.ts.
 */

import { MIN_ORDER_M3 } from './pricing';
import { SHIPPING_HOURS } from './company';

export interface Fact {
  /** Подпись факта. */
  label: string;
  /** Сам факт. */
  value: string;
}

export const FACTS: Fact[] = [
  { label: 'Площадка', value: `Люберцы, ${SHIPPING_HOURS}` },
  { label: 'Минимальная отгрузка', value: `${MIN_ORDER_M3} м³ — одна машина` },
  { label: 'Документы', value: 'Паспорт качества на каждую партию' },
];
