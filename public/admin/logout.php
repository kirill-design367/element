<?php
declare(strict_types=1);

require_once __DIR__ . '/lib/auth.php';

/**
 * ВЫХОД.
 *
 * Сессия уничтожается целиком, вместе с печеньем. Выход по времени
 * устроен там же, в is_logged_in(): час без действий — и сессия считается
 * чужой. Админку открывают с телефона прямо на площадке и оставляют
 * открытой, поэтому одной кнопки мало.
 */
require_https();
/* Тела у выхода нет — только перенаправление, индексировать здесь нечего.
   Заголовки всё равно шлём: правило «ни одна страница админки не уходит в
   поиск» не должно иметь исключений, о которых надо помнить. */
send_headers();
start_session();
logout();
header('Location: login.php');
